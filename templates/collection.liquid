<div class="container">
  <div class="collection">
    <h1>{{ collection.title }} testing</h1>
    <div class="collection-list">
      {% render 'product-card' for collection.products as product %}
    </div>
  </div>
</div>
{% section 'hero' %}
